int countDigits(int n){
	// Write your code here.	
	int num=n;
	int c=0;
	while(n>0)
	{
		int d=n%10;
                if (d > 0) {
                  if (num % d == 0)
                    c++;
                }
                n=n/10;
	}

	return c;
}
