#include <bits/stdc++.h>
vector<int> countFrequency(int n, int x, vector<int> &nums){
    // Write your code here.
    map<int,int>mpp;
    for(int i=0;i<n;i++)
    {
        mpp[nums[i]]++;
    }
    vector<int>v;
    for(int i=1;i<=n;i++)
    {
        v.push_back(mpp[i]);
    }
    return v;
}
